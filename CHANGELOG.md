# Changelog

## 0.8.1 - 2026-08-15

- Accept the current schema-bound image tools and forward bounded media-model search limits through the standalone driver.
- Reconcile terminal Codex image history without weakening exact media binding or callback integrity.
- Add explicit `starlight auth logout --local-only` recovery for stale local credentials while preserving revoke-first logout by default.

## 0.8.0 - 2026-08-11

- Initial macOS CLI extraction with Keychain pairing, a per-user Codex LaunchAgent, machine-readable diagnostics, and driver protocol `1.0.0`.
- Preserve exact server-owned media discovery schemas, opaque runtime-neutral schema bindings, and typed callback failures across the Codex App Server boundary.
- Publish the protocol compatibility fixture that protects current and supported legacy media result transport.
